# Database Configuration
# For local development with Docker: use localhost
# For Docker container: use postgres (service name)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
# ⚠️ Set your password here - this will be used by both Docker and Backend
DB_PASSWORD=postgres
DB_NAME=jobmate_db

# Application Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Password Configuration
BCRYPT_ROUNDS=10
